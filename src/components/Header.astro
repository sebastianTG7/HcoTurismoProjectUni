---
const sessionCookie = Astro.cookies.get('session');
let user = null;

if (sessionCookie) {
    try {
        user = JSON.parse(sessionCookie.value);
    } catch (e) {
        // Invalid session
    }
}
---

<header id="navbar">
    <nav>
        <div class="nav-left">
            <a href="/" class="logo">
                <span class="logo-icon">🌿</span>
                HUÁNUCO
            </a>
        </div>
        <div class="nav-center">
            <ul>
				<li><a href="/">INICIO</a></li>
                <li><a href="/historia">HISTORIA</a></li>
                <li><a href="/naturaleza">NATURALEZA</a></li>
                <li><a href="/turismo">TURISMO</a></li>
                <li><a href="/cultura">CULTURA</a></li>
				<li><a href="/gastronomia">GASTRONOMÍA</a></li>
                <li><a href="/contacto">CONTACTO</a></li>
            </ul>
        </div>
        <div class="nav-right">
            {user ? (
                <div class="user-menu">
                    <button class="welcome-btn" id="userMenuBtn">
                        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
                            <circle cx="12" cy="7" r="4"></circle>
                        </svg>
                        <span class="welcome-text">Bienvenido </span><span class="user-name-text">{user.nombre}</span>
                        <svg class="chevron" xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <polyline points="6 9 12 15 18 9"></polyline>
                        </svg>
                    </button>
                    <div class="user-dropdown" id="userDropdown">
                        {user.rol === 'admin' && (
                            <a href="/admin" class="dropdown-item admin-item">
                                <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M12 2L2 7l10 5 10-5-10-5z"></path>
                                    <path d="M2 17l10 5 10-5"></path>
                                    <path d="M2 12l10 5 10-5"></path>
                                </svg>
                                Panel Administrador
                            </a>
                        )}
                        <a href="#" id="changePasswordBtn" class="dropdown-item password-item">
                            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect>
                                <path d="M7 11V7a5 5 0 0 1 10 0v4"></path>
                            </svg>
                            Cambiar Contraseña
                        </a>
                        <a href="#" id="desktopLogoutBtn" class="dropdown-item logout-item">
                            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path>
                                <polyline points="16 17 21 12 16 7"></polyline>
                                <line x1="21" y1="12" x2="9" y2="12"></line>
                            </svg>
                            Cerrar Sesión
                        </a>
                    </div>
                </div>
            ) : (
                <a href="/login" class="login-btn">
                    <span>INICIAR SESIÓN</span>
                </a>
            )}
            <div class="hamburger-menu" id="hamburger">
                <span></span>
                <span></span>
                <span></span>
            </div>
        </div>
    </nav>
    <div class="mobile-menu" id="mobileMenu">
        <ul>
            {user ? (
                <>
                    <li class="mobile-user-info">
                        <div class="user-avatar">
                            <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
                                <circle cx="12" cy="7" r="4"></circle>
                            </svg>
                        </div>
                        <div class="user-details">
                            <span class="user-greeting">Bienvenido</span>
                            <span class="user-name">{user.nombre}</span>
                        </div>
                    </li>
                    <li class="menu-divider"></li>
                    <li><a href="/">INICIO</a></li>
                    <li><a href="/historia">HISTORIA</a></li>
                    <li><a href="/naturaleza">NATURALEZA</a></li>
                    <li><a href="/turismo">TURISMO</a></li>
                    <li><a href="/cultura">CULTURA</a></li>
                    <li><a href="/gastronomia">GASTRONOMÍA</a></li>
                    <li><a href="/contacto" class="mobile-contact">CONTACTO</a></li>
                    <li class="menu-divider"></li>
                    <li><a href="#" id="logoutBtn" class="mobile-logout">CERRAR SESIÓN</a></li>
                </>
            ) : (
                <>
                    <li><a href="/">INICIO</a></li>
                    <li><a href="/historia">HISTORIA</a></li>
                    <li><a href="/naturaleza">NATURALEZA</a></li>
                    <li><a href="/turismo">TURISMO</a></li>
                    <li><a href="/cultura">CULTURA</a></li>
                    <li><a href="/gastronomia">GASTRONOMÍA</a></li>
                    <li><a href="/contacto" class="mobile-contact">CONTACTO</a></li>
                    <li><a href="/login" class="mobile-login">INICIAR SESIÓN</a></li>
                </>
            )}
        </ul>
    </div>
</header>

<!-- Modal de cambio de contraseña -->
<div id="changePasswordModal" class="modal">
    <div class="modal-content">
        <h2>Cambiar Contraseña</h2>
        <form id="changePasswordForm">
            <div class="form-group">
                <label>Contraseña Actual</label>
                <input type="password" id="currentPassword" required />
            </div>
            <div class="form-group">
                <label>Nueva Contraseña</label>
                <input type="password" id="newPassword" required />
            </div>
            <div class="form-group">
                <label>Confirmar Nueva Contraseña</label>
                <input type="password" id="confirmNewPassword" required />
            </div>
            <div class="modal-buttons">
                <button type="submit" class="btn-confirm">Guardar</button>
                <button type="button" id="cancelChangePassword" class="btn-cancel">Cancelar</button>
            </div>
        </form>
        <div id="passwordMessage" class="password-message"></div>
    </div>
</div>

<!-- Modal de confirmación de cierre de sesión -->
<div id="logoutModal" class="modal">
    <div class="modal-content">
        <h2>Cerrar Sesión</h2>
        <p>¿Estás seguro que quieres cerrar sesión?</p>
        <div class="modal-buttons">
            <button id="confirmLogout" class="btn-confirm">Sí, cerrar sesión</button>
            <button id="cancelLogout" class="btn-cancel">Cancelar</button>
        </div>
    </div>
</div>

<script is:inline>
    document.addEventListener('DOMContentLoaded', () => {
        let lastScrollTop = 0;
        const navbar = document.getElementById('navbar');
        const scrollThreshold = 100;
        const hamburger = document.getElementById('hamburger');
        const mobileMenu = document.getElementById('mobileMenu');
        const logoutModal = document.getElementById('logoutModal');
        const logoutBtn = document.getElementById('logoutBtn');
        const desktopLogoutBtn = document.getElementById('desktopLogoutBtn');
        const confirmLogout = document.getElementById('confirmLogout');
        const cancelLogout = document.getElementById('cancelLogout');
        const userMenuBtn = document.getElementById('userMenuBtn');
        const userDropdown = document.getElementById('userDropdown');
        const changePasswordBtn = document.getElementById('changePasswordBtn');
        const changePasswordModal = document.getElementById('changePasswordModal');
        const cancelChangePassword = document.getElementById('cancelChangePassword');
        const changePasswordForm = document.getElementById('changePasswordForm');
        const passwordMessage = document.getElementById('passwordMessage');
        
        // Scroll functionality
        window.addEventListener('scroll', () => {
            const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
            
            if (scrollTop > scrollThreshold) {
                if (scrollTop > lastScrollTop) {
                    // Scrolling down
                    navbar.classList.add('hidden');
                    navbar.classList.remove('visible');
                } else {
                    // Scrolling up
                    navbar.classList.remove('hidden');
                    navbar.classList.add('visible');
                }
            } else {
                // At top of page
                navbar.classList.remove('hidden');
                navbar.classList.remove('visible');
            }
            
            lastScrollTop = scrollTop <= 0 ? 0 : scrollTop;
        });

        // User menu dropdown (desktop)
        if (userMenuBtn && userDropdown) {
            userMenuBtn.addEventListener('click', (e) => {
                e.stopPropagation();
                userDropdown.classList.toggle('active');
            });

            // Close dropdown when clicking outside
            document.addEventListener('click', (e) => {
                if (!userDropdown.contains(e.target) && !userMenuBtn.contains(e.target)) {
                    userDropdown.classList.remove('active');
                }
            });
        }

        // Mobile menu toggle
        hamburger.addEventListener('click', () => {
            hamburger.classList.toggle('active');
            mobileMenu.classList.toggle('active');
            document.body.classList.toggle('menu-open');
        });

        // Close menu when clicking on a link
        const mobileLinks = mobileMenu.querySelectorAll('a:not(#logoutBtn)');
        mobileLinks.forEach(link => {
            link.addEventListener('click', () => {
                hamburger.classList.remove('active');
                mobileMenu.classList.remove('active');
                document.body.classList.remove('menu-open');
            });
        });

        // Close menu when clicking outside
        document.addEventListener('click', (e) => {
            if (!navbar.contains(e.target) && !logoutModal.contains(e.target)) {
                hamburger.classList.remove('active');
                mobileMenu.classList.remove('active');
                document.body.classList.remove('menu-open');
            }
        });

        // Logout functionality - Mobile
        if (logoutBtn) {
            logoutBtn.addEventListener('click', (e) => {
                e.preventDefault();
                logoutModal.classList.add('active');
                document.body.classList.add('modal-open');
            });
        }

        // Logout functionality - Desktop
        if (desktopLogoutBtn) {
            desktopLogoutBtn.addEventListener('click', (e) => {
                e.preventDefault();
                userDropdown.classList.remove('active');
                logoutModal.classList.add('active');
                document.body.classList.add('modal-open');
            });
        }

        if (confirmLogout) {
            confirmLogout.addEventListener('click', async () => {
                try {
                    const response = await fetch('/api/auth/logout', {
                        method: 'POST'
                    });
                    
                    if (response.ok) {
                        window.location.href = '/';
                    }
                } catch (error) {
                    console.error('Error al cerrar sesión:', error);
                }
            });
        }

        if (cancelLogout) {
            cancelLogout.addEventListener('click', () => {
                logoutModal.classList.remove('active');
                document.body.classList.remove('modal-open');
            });
        }

        // Change Password functionality
        if (changePasswordBtn) {
            changePasswordBtn.addEventListener('click', (e) => {
                e.preventDefault();
                userDropdown?.classList.remove('active');
                changePasswordModal.classList.add('active');
                document.body.classList.add('modal-open');
            });
        }

        if (cancelChangePassword) {
            cancelChangePassword.addEventListener('click', () => {
                changePasswordModal.classList.remove('active');
                document.body.classList.remove('modal-open');
                changePasswordForm.reset();
                passwordMessage.style.display = 'none';
            });
        }

        if (changePasswordForm) {
            changePasswordForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                
                const currentPassword = document.getElementById('currentPassword').value;
                const newPassword = document.getElementById('newPassword').value;
                const confirmNewPassword = document.getElementById('confirmNewPassword').value;

                if (newPassword !== confirmNewPassword) {
                    passwordMessage.textContent = 'Las nuevas contraseñas no coinciden';
                    passwordMessage.className = 'password-message error';
                    return;
                }

                if (newPassword.length < 4) {
                    passwordMessage.textContent = 'La contraseña debe tener al menos 4 caracteres';
                    passwordMessage.className = 'password-message error';
                    return;
                }

                try {
                    const response = await fetch('/api/auth/change-password', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({
                            currentPassword,
                            newPassword
                        })
                    });

                    const data = await response.json();

                    if (response.ok) {
                        passwordMessage.textContent = '¡Contraseña actualizada exitosamente!';
                        passwordMessage.className = 'password-message success';
                        changePasswordForm.reset();
                        
                        setTimeout(() => {
                            changePasswordModal.classList.remove('active');
                            document.body.classList.remove('modal-open');
                            passwordMessage.style.display = 'none';
                        }, 2000);
                    } else {
                        passwordMessage.textContent = data.message || 'Error al cambiar contraseña';
                        passwordMessage.className = 'password-message error';
                    }
                } catch (error) {
                    passwordMessage.textContent = 'Error de conexión';
                    passwordMessage.className = 'password-message error';
                }
            });
        }

        // Close modal when clicking outside
        logoutModal?.addEventListener('click', (e) => {
            if (e.target === logoutModal) {
                logoutModal.classList.remove('active');
                document.body.classList.remove('modal-open');
            }
        });

        changePasswordModal?.addEventListener('click', (e) => {
            if (e.target === changePasswordModal) {
                changePasswordModal.classList.remove('active');
                document.body.classList.remove('modal-open');
                changePasswordForm.reset();
                passwordMessage.style.display = 'none';
            }
        });
    });
</script>

<style>
header {
    width: 100%;
    position: fixed;
    top: 0;
    left: 0;
    z-index: 1000;
    background: linear-gradient(to bottom, rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.3));
    backdrop-filter: blur(5px);
    transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1), 
                background 0.4s ease,
                box-shadow 0.4s ease;
    border-bottom: 1px solid rgba(16, 185, 129, 0.1);
}

header::before {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 1px;
    background: linear-gradient(90deg, 
        transparent, 
        rgba(16, 185, 129, 0.5) 50%, 
        transparent);
    opacity: 0;
    transition: opacity 0.4s ease;
}

header.visible {
    background: rgba(6, 78, 59, 0.95);
    backdrop-filter: blur(15px);
    box-shadow: 
        0 4px 20px rgba(0, 0, 0, 0.3),
        0 0 40px rgba(16, 185, 129, 0.1);
}

header.visible::before {
    opacity: 1;
}

header.hidden {
    transform: translateY(-100%);
}

nav {
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    gap: 40px;
    padding: 18px 40px;
    color: white;
    max-width: 1600px;
    margin: 0 auto;
}

.nav-left {
    display: flex;
    align-items: center;
}

.nav-center {
    display: flex;
    justify-content: center;
    flex: 1;
}

.nav-right {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 20px;
}

.logo-icon {
    font-size: 30px;
    filter: drop-shadow(0 0 10px rgba(16, 185, 129, 0.6));
    animation: leafPulse 3s ease-in-out infinite;
}

.logo:hover {
    color: #10b981;
    text-shadow: 0 0 30px rgba(16, 185, 129, 0.8);
}

@keyframes leafPulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.1); }
}

.nav-center ul {
    display: flex;
    list-style: none;
    gap: 35px;
    margin: 0;
    padding: 0;
}

.nav-center a {
    text-decoration: none;
    color: #e0e0e0;
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 1.8px;
    padding: 10px 0;
    position: relative;
    transition: all 0.3s ease;
    white-space: nowrap;
}

.nav-center a::before {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    width: 0;
    height: 2px;
    background: linear-gradient(90deg, #10b981, #34d399);
    transform: translateX(-50%);
    transition: width 0.3s ease;
    box-shadow: 0 0 10px rgba(16, 185, 129, 0.6);
}

.nav-center a:hover {
    color: #10b981;
    text-shadow: 0 0 15px rgba(16, 185, 129, 0.5);
}

.nav-center a:hover::before {
    width: 100%;
}

.nav-center a.active {
    color: #10b981;
}

.nav-center a.active::before {
    width: 100%;
}

.logo {
    font-size: 26px;
    font-weight: 700;
    text-decoration: none;
    color: #ffffff;
    letter-spacing: 2.5px;
    display: flex;
    align-items: center;
    gap: 10px;
    text-shadow: 0 0 20px rgba(16, 185, 129, 0.4);
    transition: all 0.3s ease;
    position: relative;
    white-space: nowrap;
}

.nav-right {
    display: flex;
    align-items: center;
    gap: 20px;
}

.login-btn {
    position: relative;
    text-decoration: none;
    color: #ffffff;
    background: linear-gradient(135deg, rgba(16, 185, 129, 0.3), rgba(5, 150, 105, 0.4));
    padding: 12px 28px;
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 1.8px;
    border: 1.5px solid rgba(16, 185, 129, 0.6);
    transition: all 0.4s ease;
    overflow: hidden;
    backdrop-filter: blur(10px);
    box-shadow: 0 0 20px rgba(16, 185, 129, 0.3);
    white-space: nowrap;
}

.user-menu {
    display: flex;
    align-items: center;
    gap: 15px;
    position: relative;
}

.welcome-btn {
    display: flex;
    align-items: center;
    gap: 10px;
    color: #ffffff;
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 1.5px;
    padding: 12px 20px;
    background: linear-gradient(135deg, rgba(16, 185, 129, 0.3), rgba(5, 150, 105, 0.4));
    border: 1.5px solid rgba(16, 185, 129, 0.6);
    border-radius: 5px;
    backdrop-filter: blur(10px);
    box-shadow: 0 0 20px rgba(16, 185, 129, 0.3);
    white-space: nowrap;
    cursor: pointer;
    transition: all 0.3s ease;
}

.welcome-btn:hover {
    background: linear-gradient(135deg, rgba(16, 185, 129, 0.4), rgba(5, 150, 105, 0.5));
    border-color: rgba(52, 211, 153, 0.8);
    box-shadow: 0 0 30px rgba(16, 185, 129, 0.5);
}

.welcome-btn svg:first-child {
    flex-shrink: 0;
}

.welcome-btn .chevron {
    flex-shrink: 0;
    transition: transform 0.3s ease;
}

.welcome-btn:hover .chevron {
    transform: translateY(2px);
}

.user-dropdown {
    position: absolute;
    top: calc(100% + 10px);
    right: 0;
    min-width: 200px;
    background: rgba(255, 255, 255, 0.98);
    backdrop-filter: blur(15px);
    border-radius: 10px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
    opacity: 0;
    visibility: hidden;
    transform: translateY(-10px);
    transition: all 0.3s ease;
    z-index: 1100;
    overflow: hidden;
}

.user-dropdown.active {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
}

.user-dropdown::before {
    content: '';
    position: absolute;
    top: -6px;
    right: 20px;
    width: 12px;
    height: 12px;
    background: white;
    transform: rotate(45deg);
    box-shadow: -2px -2px 5px rgba(0, 0, 0, 0.1);
}

.dropdown-item {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 15px 20px;
    color: #064e3b;
    text-decoration: none;
    font-size: 14px;
    font-weight: 600;
    transition: all 0.3s ease;
    border-bottom: 1px solid rgba(16, 185, 129, 0.1);
}

.dropdown-item:last-child {
    border-bottom: none;
}

.dropdown-item:hover {
    background: linear-gradient(135deg, rgba(16, 185, 129, 0.1), rgba(5, 150, 105, 0.1));
}

.logout-item {
    color: #dc2626;
}

.logout-item:hover {
    background: linear-gradient(135deg, rgba(239, 68, 68, 0.1), rgba(220, 38, 38, 0.1));
}

.logout-item svg {
    color: #dc2626;
}

.admin-item {
    color: #10b981;
    border-bottom: 1px solid #e2e8f0;
}

.admin-item:hover {
    background: linear-gradient(135deg, rgba(16, 185, 129, 0.1), rgba(5, 150, 105, 0.1));
}

.admin-item svg {
    color: #10b981;
}

.password-item {
    color: #3b82f6;
    border-bottom: 1px solid #e2e8f0;
}

.password-item:hover {
    background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(37, 99, 235, 0.1));
}

.password-item svg {
    color: #3b82f6;
}

.welcome-text {
    display: none;
}
    background: linear-gradient(135deg, rgba(16, 185, 129, 0.3), rgba(5, 150, 105, 0.4));
    border: 1.5px solid rgba(16, 185, 129, 0.6);
    border-radius: 5px;
    backdrop-filter: blur(10px);
    box-shadow: 0 0 20px rgba(16, 185, 129, 0.3);
    white-space: nowrap;
}

.login-btn::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 0;
    height: 0;
    background: rgba(16, 185, 129, 0.3);
    border-radius: 50%;
    transform: translate(-50%, -50%);
    transition: width 0.5s ease, height 0.5s ease;
}

.login-btn span {
    position: relative;
    z-index: 1;
}

.login-btn:hover {
    background: linear-gradient(135deg, rgba(16, 185, 129, 0.5), rgba(5, 150, 105, 0.6));
    border-color: rgba(52, 211, 153, 1);
    transform: translateY(-2px);
    box-shadow: 
        0 0 30px rgba(16, 185, 129, 0.6),
        0 5px 15px rgba(0, 0, 0, 0.3);
}

.login-btn:hover::before {
    width: 300px;
    height: 300px;
}

.hamburger-menu {
    cursor: pointer;
    display: none;
    flex-direction: column;
    gap: 6px;
    padding: 8px;
    position: relative;
    z-index: 10001;
}

.hamburger-menu span {
    display: block;
    width: 28px;
    height: 2px;
    background: linear-gradient(90deg, #10b981, #34d399);
    transition: all 0.3s ease;
    box-shadow: 0 0 8px rgba(16, 185, 129, 0.5);
}

.hamburger-menu:hover span {
    box-shadow: 0 0 15px rgba(16, 185, 129, 0.8);
}

.hamburger-menu.active span:nth-child(1) {
    transform: rotate(45deg) translate(8px, 8px);
}

.hamburger-menu.active span:nth-child(2) {
    opacity: 0;
}

.hamburger-menu.active span:nth-child(3) {
    transform: rotate(-45deg) translate(8px, -8px);
}

.mobile-menu {
    position: fixed;
    top: 0;
    right: -100%;
    width: 80%;
    max-width: 350px;
    height: 100vh;
    background: linear-gradient(135deg,
        rgba(6, 78, 59, 0.98) 0%,
        rgba(5, 150, 105, 0.98) 100%);
    backdrop-filter: blur(20px);
    padding-top: 100px;
    transition: right 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: -10px 0 40px rgba(0, 0, 0, 0.5);
    border-left: 1px solid rgba(16, 185, 129, 0.3);
    z-index: 10000;
}

.mobile-menu::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: 
        linear-gradient(rgba(16, 185, 129, 0.05) 1px, transparent 1px),
        linear-gradient(90deg, rgba(16, 185, 129, 0.05) 1px, transparent 1px);
    background-size: 40px 40px;
    pointer-events: none;
}

.mobile-menu.active {
    right: 0;
}

.mobile-menu ul {
    list-style: none;
    padding: 0 30px;
}

.mobile-menu li {
    margin-bottom: 10px;
    opacity: 0;
    transform: translateX(50px);
    transition: all 0.3s ease;
}

.mobile-menu.active li {
    opacity: 1;
    transform: translateX(0);
}

.mobile-menu.active li:nth-child(1) { transition-delay: 0s; }
.mobile-menu.active li:nth-child(2) { transition-delay: 0.05s; }
.mobile-menu.active li:nth-child(3) { transition-delay: 0.1s; }
.mobile-menu.active li:nth-child(4) { transition-delay: 0.15s; }
.mobile-menu.active li:nth-child(5) { transition-delay: 0.2s; }
.mobile-menu.active li:nth-child(6) { transition-delay: 0.25s; }
.mobile-menu.active li:nth-child(7) { transition-delay: 0.3s; }
.mobile-menu.active li:nth-child(8) { transition-delay: 0.35s; }
.mobile-menu.active li:nth-child(9) { transition-delay: 0.4s; }
.mobile-menu.active li:nth-child(10) { transition-delay: 0.45s; }
.mobile-menu.active li:nth-child(11) { transition-delay: 0.5s; }

.mobile-menu a {
    display: block;
    text-decoration: none;
    color: #ffffff;
    font-size: 16px;
    font-weight: 600;
    letter-spacing: 2px;
    padding: 18px 20px;
    position: relative;
    transition: all 0.3s ease;
    border-bottom: 1px solid rgba(16, 185, 129, 0.2);
}

.mobile-menu a::before {
    content: '';
    position: absolute;
    left: 0;
    top: 50%;
    transform: translateY(-50%);
    width: 0;
    height: 100%;
    background: rgba(16, 185, 129, 0.2);
    transition: width 0.3s ease;
    border-left: 3px solid #10b981;
}

.mobile-menu a:hover::before,
.mobile-menu a.active::before {
    width: 100%;
}

.mobile-menu a:hover,
.mobile-menu a.active {
    color: #10b981;
    text-shadow: 0 0 15px rgba(16, 185, 129, 0.6);
    padding-left: 30px;
}

.mobile-contact {
    margin-top: 20px;
    background: linear-gradient(135deg, rgba(16, 185, 129, 0.3), rgba(5, 150, 105, 0.4));
    border: 1.5px solid rgba(16, 185, 129, 0.6) !important;
    text-align: center;
    box-shadow: 0 0 20px rgba(16, 185, 129, 0.3);
}

.mobile-contact:hover {
    background: linear-gradient(135deg, rgba(16, 185, 129, 0.5), rgba(5, 150, 105, 0.6));
    box-shadow: 0 0 30px rgba(16, 185, 129, 0.6);
}

.mobile-login {
    margin-top: 10px;
    background: linear-gradient(135deg, rgba(16, 185, 129, 0.3), rgba(5, 150, 105, 0.4));
    border: 1.5px solid rgba(16, 185, 129, 0.6) !important;
    text-align: center;
    box-shadow: 0 0 20px rgba(16, 185, 129, 0.3);
}

.mobile-login:hover {
    background: linear-gradient(135deg, rgba(16, 185, 129, 0.5), rgba(5, 150, 105, 0.6));
    box-shadow: 0 0 30px rgba(16, 185, 129, 0.6);
}

.mobile-user-info {
    margin-top: 0;
    padding: 25px 20px;
    background: linear-gradient(135deg, rgba(16, 185, 129, 0.25), rgba(5, 150, 105, 0.35));
    border: none !important;
    border-bottom: 2px solid rgba(16, 185, 129, 0.4) !important;
    display: flex;
    align-items: center;
    gap: 15px;
    opacity: 1 !important;
    transform: translateX(0) !important;
}

.user-avatar {
    width: 50px;
    height: 50px;
    background: linear-gradient(135deg, #10b981, #059669);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 4px 15px rgba(16, 185, 129, 0.4);
    flex-shrink: 0;
}

.user-avatar svg {
    color: white;
}

.user-details {
    display: flex;
    flex-direction: column;
    gap: 3px;
}

.user-greeting {
    color: rgba(255, 255, 255, 0.8);
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 0.5px;
}

.user-name {
    color: #ffffff;
    font-weight: 700;
    font-size: 16px;
    letter-spacing: 0.5px;
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
}

.menu-divider {
    height: 1px;
    background: linear-gradient(90deg, 
        transparent, 
        rgba(16, 185, 129, 0.3) 50%, 
        transparent);
    margin: 10px 0;
    opacity: 1 !important;
    transform: translateX(0) !important;
}

.mobile-logout {
    margin-top: 5px;
    background: linear-gradient(135deg, rgba(239, 68, 68, 0.3), rgba(220, 38, 38, 0.4));
    border: 1.5px solid rgba(239, 68, 68, 0.6) !important;
    text-align: center;
    box-shadow: 0 0 20px rgba(239, 68, 68, 0.3);
}

.mobile-logout:hover {
    background: linear-gradient(135deg, rgba(239, 68, 68, 0.5), rgba(220, 38, 38, 0.6));
    box-shadow: 0 0 30px rgba(239, 68, 68, 0.6);
}

/* Modal styles */
.modal {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(5px);
    z-index: 2000;
    align-items: center;
    justify-content: center;
}

.modal.active {
    display: flex;
    animation: fadeIn 0.3s ease;
}

.modal-content {
    background: white;
    padding: 40px;
    border-radius: 20px;
    max-width: 400px;
    width: 90%;
    text-align: center;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
    animation: slideUp 0.3s ease;
}

@keyframes slideUp {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.modal-content h2 {
    color: #064e3b;
    font-size: 24px;
    margin-bottom: 15px;
    font-weight: 700;
}

.modal-content p {
    color: #059669;
    font-size: 16px;
    margin-bottom: 25px;
}

.modal-buttons {
    display: flex;
    gap: 15px;
    justify-content: center;
}

.modal-buttons button {
    padding: 12px 25px;
    border: none;
    border-radius: 10px;
    font-size: 14px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    text-transform: uppercase;
    letter-spacing: 1px;
}

.btn-confirm {
    background: linear-gradient(135deg, #ef4444, #dc2626);
    color: white;
}

.btn-confirm:hover {
    background: linear-gradient(135deg, #dc2626, #b91c1c);
    transform: translateY(-2px);
    box-shadow: 0 10px 25px rgba(239, 68, 68, 0.3);
}

.btn-cancel {
    background: linear-gradient(135deg, #10b981, #059669);
    color: white;
}

.btn-cancel:hover {
    background: linear-gradient(135deg, #059669, #047857);
    transform: translateY(-2px);
    box-shadow: 0 10px 25px rgba(16, 185, 129, 0.3);
}

.form-group {
    margin-bottom: 1.5rem;
}

.form-group label {
    display: block;
    color: #064e3b;
    font-weight: 600;
    margin-bottom: 0.5rem;
}

.form-group input {
    width: 100%;
    padding: 0.8rem;
    border: 2px solid #e2e8f0;
    border-radius: 8px;
    font-size: 1rem;
    transition: border-color 0.3s;
}

.form-group input:focus {
    outline: none;
    border-color: #10b981;
}

.password-message {
    margin-top: 1rem;
    padding: 0.8rem;
    border-radius: 8px;
    text-align: center;
    font-weight: 600;
    display: none;
}

.password-message.success {
    background: #d1fae5;
    color: #065f46;
    display: block;
}

.password-message.error {
    background: #fee2e2;
    color: #991b1b;
    display: block;
}

body.modal-open {
    overflow: hidden;
}

body.menu-open {
    overflow: hidden;
}

body.menu-open::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.7);
    backdrop-filter: blur(5px);
    z-index: 998;
    animation: fadeIn 0.3s ease;
}

@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

/* === BREAKPOINTS RESPONSIVE === */

/* Pantallas muy grandes (laptops XL) */
@media (max-width: 1400px) {
    nav {
        padding: 18px 35px;
        gap: 32px;
    }

    .nav-center ul {
        gap: 20px;
    }

    .nav-center a {
        font-size: 13px;
        padding: 10px 15px;
    }
}

/* Laptops estándar */
@media (max-width: 1200px) {
    nav {
        padding: 18px 30px;
        gap: 28px;
    }
    
    .nav-center ul {
        gap: 16px;
    }

    .nav-center a {
        font-size: 12px;
        padding: 10px 12px;
        letter-spacing: 1.5px;
    }

    .logo {
        font-size: 24px;
        letter-spacing: 2px;
    }

    .logo-icon {
        font-size: 28px;
    }

    .welcome-btn {
        font-size: 12px;
        padding: 10px 15px;
    }

    .login-btn {
        padding: 10px 18px;
        font-size: 13px;
    }
}

/* Tablets grandes y laptops pequeños */
@media (max-width: 1024px) {
    nav {
        gap: 20px;
        padding: 16px 25px;
    }

    .nav-center ul {
        gap: 12px;
    }

    .nav-center a {
        font-size: 11px;
        padding: 10px 10px;
    }

    .logo {
        font-size: 22px;
    }

    .logo-icon {
        font-size: 26px;
    }
}

/* Tablets */
@media (max-width: 768px) {
    nav {
        grid-template-columns: auto 1fr;
        padding: 16px 15px;
        gap: 10px;
    }

    .nav-right {
        display: flex;
        justify-content: flex-end;
        align-items: center;
        gap: 8px;
    }
    
    .nav-center {
        display: none;
    }
    
    .hamburger-menu {
        display: flex;
        flex-shrink: 0;
    }
    
    .logo {
        font-size: 22px;
    }
    
    .logo-icon {
        font-size: 26px;
    }
    
    .login-btn {
        padding: 10px 20px;
        font-size: 12px;
        letter-spacing: 1.5px;
    }

    .welcome-btn {
        font-size: 11px;
        padding: 10px 12px;
        gap: 6px;
        letter-spacing: 0.5px;
        max-width: 160px;
    }

    .welcome-text {
        display: none;
    }

    .user-name-text {
        max-width: 80px;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    .welcome-btn svg:first-child {
        width: 16px;
        height: 16px;
        flex-shrink: 0;
    }

    .welcome-btn .chevron {
        width: 14px;
        height: 14px;
        flex-shrink: 0;
    }
}

@media (max-width: 480px) {
    nav {
        padding: 12px 10px;
    }

    .logo {
        font-size: 18px;
    }

    .logo-icon {
        font-size: 22px;
    }

    .welcome-btn {
        font-size: 10px;
        padding: 8px 10px;
        max-width: 120px;
    }

    .user-name-text {
        max-width: 60px;
    }

    .login-btn {
        padding: 8px 15px;
        font-size: 11px;
    }
}

@media (max-width: 480px) {
    .login-btn,
    .welcome-btn {
        display: none;
    }

    .mobile-menu {
        width: 100%;
        max-width: 100%;
    }
}
</style>